# API Configuration Multi-Tenant
# Port de l'API backend
VITE_API_PORT=8000

# Domaine de base (sans sous-domaine)
VITE_API_BASE_DOMAIN=localhost

# Timeout pour les requêtes API (en millisecondes)
# 60 secondes pour éviter les timeouts sur les requêtes lentes (login, rapports, etc.)
VITE_API_TIMEOUT=60000

# Note: La baseURL est construite dynamiquement selon le sous-domaine
# Ex: localhost:5174 → http://localhost:8000/api/v1/ (super admin)
#     santa.localhost:5174 → http://santa.localhost:8000/api/v1/ (tenant)
